import 'package:flutter/material.dart';

class DetailClubPage extends StatelessWidget {
  const DetailClubPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
